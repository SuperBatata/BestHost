{% extends  'base.html.twig' %}


{% block body %}


    {% if app.user %}
        <h1> {{ user.first_name }}</h1>
        <h1> {{ user.last_name }}</h1>
    <img src="{{ user.avatar }}">
    {% endif %}



{% endblock %}